/**
 * Default content for the @modal parallel slot when no intercepted route
 * is active. Returning null means the slot is empty (no overlay).
 */
export default function ModalDefault() {
  return null;
}
